# Use a Java 17 base image (match your Spring Boot version)
FROM eclipse-temurin:17-jdk-alpine

# Set working directory inside container
WORKDIR /app

# Copy the Maven/Gradle build JAR file (adjust filename)
COPY target/expense-tracker-0.0.1-SNAPSHOT.jar app.jar

# Expose port (match server.port in application.properties if set)
EXPOSE 8080

# Command to run the JAR
ENTRYPOINT ["java", "-jar", "app.jar"]
